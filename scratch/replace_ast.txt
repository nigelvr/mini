    '''
    XXX Not needed now
    def replace_symbol(self, old, new):
        for k in range(0, len(self.children)):
            if isinstance(self.children[k], IdentAST):
                if self.children[k].root == old:
                    self.children[k].root = new
            else:
                if isinstance(self.children[k], AST):
                    self.children[k].replace_symbol(old, new)
    '''